这里边可以有各种游戏：

1. PVP 的游戏
2. 一个人玩的生产建造类游戏（比如每隔一段时间进来点一下）

## 技术约束与架构规范

1. **多实例部署**：服务器采用多实例部署（例如 PM2 Cluster 模式），因此**严禁在内存中存储需要跨进程共享的游戏状态**。必须假设每个请求可能由不同的进程处理。
2. **数据库一致性**：所有数据库写操作必须实时持久化到数据库。读取关键状态（如游戏余额、投注列表、当前轮次等）时，必须从数据库进行同步，不能依赖本地内存缓存，以确保多实例间的数据强一致性。
